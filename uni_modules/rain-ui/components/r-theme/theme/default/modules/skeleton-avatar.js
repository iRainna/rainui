import { computed } from "vue";

const skeletonAvatarData = computed(() => ({
  "r-skeleton-avatar-size": "64rpx",
  "r-skeleton-avatar-background": "var(--r-active-color)",
}));
export default skeletonAvatarData;
